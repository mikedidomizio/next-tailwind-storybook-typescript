# A basic implementation of Next + Tailwind + Storybook + Typescript

This was just a test to see it in action with this configuration

```
# install dependencies
yarn install

# run the application
yarn dev

# storybook
yarn storybook
```
